image sdcard.img {
	hdimage {
		partition-table-type = gpt
	}

	partition loader1 {
		image = "idbloader.img"
		offset = 32K
	}

	partition loader2 {
		image = "u-boot.itb"
		offset = 8M
	}

	partition rootfs {
		offset = 16M
		image = "rootfs.ext4"
		partition-uuid = %PARTUUID%
	}
}
